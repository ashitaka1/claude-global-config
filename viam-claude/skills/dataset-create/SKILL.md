---
name: dataset-create
description: Create a new Viam dataset
argument-hint: <org-id> <dataset-name>
---

Create a dataset with: $ARGUMENTS

```bash
viam dataset create --org-id=<org-id> --name=<dataset-name>
```

After creation, the dataset ID will be displayed for use in config.